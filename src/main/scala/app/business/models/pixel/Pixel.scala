package app.business.models.pixel

/**
 * Generic interface that represents a pixel.
 *
 * Concrete pixels can be: RGBPixel (with red, green, blue values), RGBAPixel (with additional transparency), ...
 * (If needed, coords can be added here)
 */
trait Pixel
